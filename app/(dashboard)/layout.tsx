export const metadata = {
  title: 'Dashboard - Helpsi',
  description: 'Painel de controle do Helpsi',
}

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
